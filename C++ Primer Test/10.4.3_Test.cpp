/*#include<iostream>  
#include<string>  
#include<vector>  
#include<algorithm>  
#include<numeric>  
#include<functional>
#include<iterator>
#include<list>
#include <fstream>
#include <sstream>

using namespace std;

/*int main1()  //Á·Ï°10.34
{
	vector<string> text{ "i","can","fly","fly","on","the","sky!" };
	for (auto iter=text.crbegin(); iter != text.crend(); ++iter)
	{
		cout << *iter << endl;
	}
	return 0;
}

int main2()  //Á·Ï°10.35
{
	vector<string> text{ "i","can","fly","fly","on","the","sky!" };
	for (auto iter = text.end(); iter != text.begin(); iter--)
	{
		cout << *(iter - 1) << endl;
	}
	return 0;
}

int main3() //Á·Ï°10.36
{
	list<int> text{ 1,2,3,0,1,0,5,0,8 };
	auto temp = find(text.rbegin(), text.rend(), 0);
	cout << &*temp << endl;
	return 0;
}

int main4()  //Á·Ï°10.37
{
	vector<int> text{ 1,2,3,0,1,0,5,0,8,10 };
	list<int> text2;
	copy(text.rbegin() + 2, text.rend() - 3,back_inserter(text2));  //·¶Î§begin±Õ£¬end¿ª
	for (auto &s : text2)
		cout << s << endl;
}*/
	