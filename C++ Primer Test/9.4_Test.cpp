/*#include <iostream>
#include <vector>
#include <deque>
#include <list>
#include <forward_list>
#include <string>
#include <iterator>
using namespace std;

int main()
{
	vector<string> svec(1048,"s");
	svec.reserve(1024);
	string word;
	svec.resize(svec.size() + svec.size() / 2);
	cout << "size is" << svec.size() << endl;
	cout << "capacity is" << svec.capacity() << endl;
}*/